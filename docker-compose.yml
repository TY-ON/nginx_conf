version: '3'

services:
  nginx:
    build: .
    ports: 
      - "8080:80"
    networks:
      cipher:
        ipv4_address: 172.30.0.2
networks:
  cipher:
    name: cipher_proxy
    ipam:
      config:
        - subnet: "172.30.0.0/24"